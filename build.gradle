// Root-level build.gradle

// AGP 8+ prefers pluginManagement in settings.gradle
// So this file stays minimal and clean.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}