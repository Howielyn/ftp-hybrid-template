buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Gradle plugin should be defined in environment (Android Studio)
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
